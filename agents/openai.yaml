interface:
  display_name: "Web Search Skill"
  short_description: "Direct web search, URL fetching, and online document lookup"
  default_prompt: "Use $web-search-skill to search the web or fetch online documents with cited sources."

policy:
  allow_implicit_invocation: true
